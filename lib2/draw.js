export default class Draw {
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.speed = 0;
        this.canvasWidth = 0;
        this.canvasHeight = 0;
    }

    draw() {
    }
}